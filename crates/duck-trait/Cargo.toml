[package]
name = "duck-trait"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Generate duck-typing accessor traits (get / set / get_mut) for fields marked with #[duck], eliminating repetitive getter/setter declarations in traits."

[lib]
proc-macro = true

[dependencies]
proc-macro2.workspace = true
quote.workspace = true
syn.workspace = true
